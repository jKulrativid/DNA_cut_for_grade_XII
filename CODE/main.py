from CODE.usage_class import *
from CODE.graphing import *
from tkinter import *
from tkinter import ttk


if __name__ == '__main__':

    # interface
    root = Tk()
    root.option_add('*Font', 'Consolas 20')
    f1 = Frame(root, bg='red')
    f1.grid(row=0, column=0, columnspan=2)
    f2 = Frame(root, bg='white')
    f2.grid(row=1, column=0)
    f3 = Frame(root, bg='white')
    f3.grid(row=1, column=1)
    L1 = Label(f1, text='DNA Cut', width=20)
    L1.pack(padx=10, pady=10)
    B1.
    root.mainloop()
